Tornado service for import of data into scarab database.
Backend for angular-application.


Add a local file nameed config.py with content:

database = dict(
    host = 'hostname',
    user = 'username',
    password = 'password'
)

secret_key = 'Some random string'